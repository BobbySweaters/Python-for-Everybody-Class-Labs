#Functions - stored and reused steps. Use the def command. This starts the definition of a function. 

big = max('Hello World')
print(big)

tiny = min('Functions')
print(tiny)

#Type conversions

float(99) / 100
i = 42
type(i)
f = float(i)
